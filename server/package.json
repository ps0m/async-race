{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.js",
  "engines": {
    "node": "16.15.0"
  },
  "devDependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "build": "npm run prod"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}